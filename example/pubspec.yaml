name: dynamic_table_example
description: This is the example app for dynamic_table package for flutter.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.2.3"
  flutter: ">=3.16.5"

dependencies:
  flutter:
    sdk: flutter
  dynamic_table:
    path: ../
  url_launcher: ^6.1.11
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/
